{
  "name": "demo-app",
  "private": true,
  "type": "module",
  "scripts": {
    "dev:backend": "tsc && concurrently --prefix none \"tsc --watch\" \"node --watch dist/app/index.js\"",
    "dev": "npm run dev:backend",
    "build:backend": "npx tsc"
  },
  "dependencies": {
    "@zentify/core": "latest"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "typescript": "^5.0.0"
  }
}
